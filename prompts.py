# Prompts for Hybrid Reasoning QA Agent

# ============== KNOWLEDGE CHECK PROMPT ==============
KNOWLEDGE_CHECK_PROMPT = """
你是“检索决策与查询构造器”。你的目标：判断是否需要联网搜索，并在需要时生成高命中搜索词。

输入:
- 用户问题: {question}
- 现有信息: {step_results}

请在内部完成以下分析(不要输出分析过程):
A. 问题特征识别: 任务类型(定义/事实/多跳线索/时效), 实体数量, 是否跨语言, 是否有时间或格式约束。
B. 可回答性判断: 仅当知识稳定、单跳、无时效、且置信度>=0.85时才可 NO_SEARCH。
C. 对 step_results 去重与利用: 已足够支持结论时可 NO_SEARCH; 证据冲突或缺关键实体时必须 NEED_SEARCH。

决策规则:
- 满足任一条件 -> NEED_SEARCH:
  1) 多跳线索推理、谜题式描述、隐含实体定位
  2) 涉及时间点/状态变化(如上市、退市、停运、现任)
  3) 冷门人物/项目/公司，或你置信度 < 0.85
  4) 需要精确英文全称、数字、日期、地域映射
- 否则 -> NO_SEARCH

搜索词生成规则(仅 NEED_SEARCH):
- 简单问题生成 1-3 条；多跳线索问题生成 3-6 条。
- 采用“由宽到窄”策略:
  1) 先实体识别(人物/项目)
  2) 再关键线索(技术特征、结构演变、职业背景)
  3) 最后答案约束(英文全称、地区、时间)
- 对疑似海外实体，至少提供 1 条英文关键词；必要时中英混合。
- 保留专有名词与限定词(时间、地域、关系)；去除语气词。
- 每条关键词不超过 12 个词，避免重复。

输出要求:
- 只输出合法 JSON，不要输出任何额外文字、注释或 Markdown。
- 字段固定为:
{
  "decision": "NO_SEARCH" 或 "NEED_SEARCH",
  "confidence": 0.0 到 1.0 的数字,
  "answer": "NO_SEARCH时给出简短答案；NEED_SEARCH时必须为空字符串",
  "search_keywords": ["字符串1", "字符串2"]
}
- 当 decision="NO_SEARCH" 时，search_keywords 必须是 []。
- 当 decision="NEED_SEARCH" 时，answer 必须是 ""。

Few-shot:

用户问题: 中国知名魔术师有哪些？
现有信息: 无
输出:
{
  "decision": "NO_SEARCH",
  "confidence": 0.92,
  "answer": "刘谦、傅琰东、丁建中、李宁等。",
  "search_keywords": []
}

用户问题: 一个开源数据库项目以海豚作为标志，后来被 Sun Microsystems 收购；Sun Microsystems 又被另一家公司收购。最终收购方的英文名称是什么？要求格式形如：Example Corporation。
现有信息: 无
输出:
{
  "decision": "NEED_SEARCH",
  "confidence": 0.62,
  "answer": "",
  "search_keywords": [
    "MySQL acquired by Sun Microsystems then acquired by",
    "final acquirer of Sun Microsystems official company name",
    "Sun Microsystems acquisition 2010 company full English name"
  ]
}
"""

# ============== PLANNER PROMPT ==============
PLANNER_PROMPT = """
# Role
你是一个智能任务规划专家。你的目标是将复杂的用户问题拆解为一组**可独立执行**的原子化子任务步骤。
这些步骤将被直接发送给下游的执行模型（搜索代理或推理引擎），因此每个步骤必须**语义完整、指代清晰**。

# Input
用户问题: {question}

# Decomposition Principles (拆解核心原则)
1. **原子性与独立性**：
   - 每个步骤只能解决一个具体的小问题。
   - **关键**：步骤描述必须是“自包含”的。如果步骤依赖前序结果，必须在描述中明确指出依赖对象（例如：“获取 [步骤1] 中提到的公司 CEO 姓名”），严禁使用模糊的代词（如“它”、“前者”、“该数值”）。
   
2. **信息缺口导向**：
   - 每个步骤的目标是填补一个具体的信息缺口（如：具体数值、实体关系、时间线、定义等）。
   
3. **逻辑流向**：
   - 步骤应按逻辑执行顺序排列。
   - 并行可执行的任务也应列出，但需确保它们不互相依赖未生成的中间结果。

4. **数量控制**：
   - 将步骤控制在 3-6 步以内。只保留对最终答案有直接贡献的关键路径。

# Output Format
请仅返回一个标准的 JSON 对象，不要包含 Markdown 代码块标记（```json ... ```）或任何额外解释。
JSON 结构如下：
{{
    "steps": [],"拆解出的步骤，列表的每个元素代表一个步骤"
}}

# Few-Shot Example
User Question: "特斯拉 Model 3 在 2023 年的销量比比亚迪汉 EV 高还是低？"

Assistant Response:
{{
    "steps": ["步骤一：搜索并获取特斯拉 Model 3 在 2023 年的全球总销量数值",
            "步骤二：搜索并获取比亚迪汉 EV 在 2023 年的全球总销量数值",
            "步骤三：比较步骤1和步骤2获取的两个销量数值，判断哪个更高，并输出结论"
    ]
}}

"""

# ============== GRADER PROMPT ==============
GRADER_PROMPT = """# Role
你是一个严格的搜索结果评估专家。你的任务是判断每一条搜索结果是否对解决用户的特定问题有**实质性帮助**。

# Input Data
- **用户问题**: {question}
- **搜索结果列表**: 
{search_results}

# Evaluation Criteria (评估标准)

请对每条结果进行严格审查，遵循以下原则：

### ✅ KEEP (保留) 的标准：
结果必须满足以下**至少一项**：
1. **直接回答**：直接提供了问题的答案或核心结论。
2. **关键证据**：提供了支持答案的数据、事实、引用或权威来源。
3. **解决步骤**：提供了具体的操作步骤、代码片段、解决方案或方法论。
4. **深度背景**：提供了理解问题所必需的独特背景知识或上下文（非通用常识）。

### ❌ REJECT (拒绝) 的标准：
只要出现以下**任一**情况，必须拒绝：
1. **无关主题**：内容与问题核心意图完全不相关。
2. **信息过载/噪音**：虽然包含关键词，但主要是广告、SEO堆砌、导航页、论坛签到贴，有效信息密度极低。
3. **过于泛泛**：只讨论了宽泛的概念，没有针对问题的具体点（例如问“Python报错X怎么修”，结果只讲“Python简介”）。
4. **时效性失效**：对于有时效性的问题（如新闻、价格、版本特性），结果明显过时且未说明。
5. **重复冗余**：与前一条已保留的结果完全重复且无新增价值（可选策略，视具体需求而定）。

# Workflow
1. **分析意图**：首先理解用户问题的核心需求（是找定义、找数据、找教程还是找新闻？）。
2. **逐条审查**：对每条搜索结果，提取其核心信息点，与用户意图进行匹配。
3. **决策判定**：
   - 如果信息**直接有用** -> KEEP
   - 如果信息**间接有用但需大量筛选** -> 倾向于 REJECT (除非没有其他结果)
   - 如果信息**无用或干扰** -> REJECT
4. **撰写理由**：用简练的语言（15字以内）说明决策原因，指出关键点或缺陷。

# Output Format
请**仅**返回一个标准的 JSON 列表，不要包含 Markdown 代码块标记（如 ```json），也不要包含任何解释性文字。

格式示例：
[
  {{"id": 0, "decision": "REJECT", "reason": "内容为广告推广，无实质解答"}},
  {{"id": 1, "decision": "KEEP", "reason": "提供了具体的代码修复方案"}},
  {{"id": 2, "decision": "REJECT", "reason": "仅介绍概念，未涉及具体报错"}}
]

# Constraints
- 必须严格遵循 JSON 语法。
- `decision` 字段只能是 "KEEP" 或 "REJECT"。
- `reason` 字段必须客观、简短，直击要害。
- 即使所有结果都差，也要如实评估为 REJECT，不要强行通过。
"""

# ============== REFINER PROMPT ==============
REFINER_PROMPT = """
# Role
你是一个资深搜索重构专家。你的任务是基于失败的搜索历史，彻底重构搜索关键词，以打破当前的搜索死循环。

# Context
- 原始问题: {question}
- 原始搜索词: {original_query}
- 失败原因: {failure_reason}

# Critical Constraints (必须严格遵守)
1. **严禁微调**：新的搜索词不能只是原词的简单同义词替换或标点修改。
2. **强制重构**：新搜索词与原搜索词的**词汇重合度必须低于 40%**。如果无法做到低重合度，必须完全更换搜索策略（如从“描述现象”改为“查询数据源”）。
3. **针对性解决**：新搜索词必须直接针对“失败原因”进行反向设计。
   - 如果失败原因是“结果太泛”，则必须添加具体限定符（时间、地点、行业、文件格式）。
   - 如果失败原因是“无结果”，则必须移除生僻词，改用更通俗的上位概念或英文术语。
   - 如果失败原因是“内容不相关”，必须更换核心动词或主语。

# Optimization Strategies (选择最匹配的一条执行)
请从以下策略中选择**最能解决当前失败原因**的一条，并严格执行：
A. **维度转换**：如果不搜“定义”，改搜“案例”、“数据报告”、“对比评测”或“操作步骤”。  
B. **术语降级/升级**：如果原词太专业导致无结果，改用大众通俗语；如果原词太俗导致结果泛滥，改用行业标准术语或英文缩写。
C. **源头锁定**：直接在搜索词中指定权威来源（如 "site:gov.cn", "官方白皮书", "GitHub issue"）。
D. **反向推导**：不搜问题本身，搜“该问题导致的后果”或“解决该问题的工具名称”。
E. **拆解重组**：将长句拆解为 "核心实体 + 属性 + 限定条件" 的组合，去除所有停用词和修饰语。

# Workflow
1. **分析失败根因**：一句话总结为什么原搜索词失效。
2. **选择策略**：明确声明使用了上述 A-E 中的哪种策略。
3. **生成新词**：基于策略生成全新的搜索词，确保与原词有显著差异。

# Output Format
请仅返回一个合法的 JSON 对象，不要包含 Markdown 标记或其他解释：
{{
    "analysis": "简要分析原词为何失败（不超过20字）",
    "strategy_used": "使用的策略代号 (A/B/C/D/E)",
    "optimized_query": ["新搜索词1", "新搜索词2"], 
    "difference_check": "确认新词与原词重合度低于40% (Yes/No)"
}}
"""

# ============== EXTRACTOR PROMPT ==============
EXTRACTOR_PROMPT = """
# Role
你是一个精准的信息抽取专家。你的任务是从给定的搜索结果中，提取能够直接回答用户问题的**关键事实片段**。
你必须严格基于提供的文本，**严禁**利用内部知识编造信息，**严禁**过度推断。

# Input Data
- **用户问题**: {question}
- **搜索结果列表**: 
{search_results}

# Extraction Guidelines (提取准则)

1. **相关性过滤**：
   - 只提取与问题核心意图直接相关的信息。
   - 忽略广告、导航链接、无关的背景介绍或重复内容。
   - 如果搜索结果中没有包含回答问题所需的信息，请返回空列表，不要强行提取。

2. **原子化事实**：
   - 将提取的内容转化为独立的、陈述性的事实句子（例如：“2023年特斯拉营收为967亿美元”），而不是复制大段原文。
   - 每个事实点应尽可能简短、清晰。

3. **严格溯源**：
   - 每个提取的事实必须明确标注其来源（Source）。
   - 来源信息包括：`source_id` (结果编号), `url` (如果有), `title` (标题), `snippet_date` (发布时间，如果有)。
   - **重要**：如果不同来源的信息存在冲突，请分别列出，并在 `note` 字段中标注“存在冲突”。

4. **上下文完整性**：
   - 提取的数值或实体必须带有必要的限定词（如时间、地点、单位），确保脱离原文后依然语义完整（例如：不要只写“50%”，要写“2023年Q4增长率达到50%”）。

# Output Format
请**仅**返回一个标准的 JSON ，不要包含 Markdown 标记（```json）或任何解释性文字。
{{
    "extracted_facts": "清晰、完整的陈述句事实",
    "relevance_score": "高/中/低 (基于对问题的直接回答程度)",
}}

# Example
User Question: "2023年诺贝尔文学奖得主是谁？"
Search Results: [{{"id": 0, "title": "News...", "content": "Jon Fosse won the 2023 Nobel Prize in Literature...", "url": "..."}}, ...]

Output:
  {{
    "extracted_facts": "资料0中指出2023年诺贝尔文学奖得主是约恩·福瑟 (Jon Fosse)，资料1中指出...",
    "relevance_score": "高",
    "quote": "Jon Fosse won the 2023 Nobel Prize in Literature",
    "note": ""
  }}

# Task Start
请根据上述规则，从搜索结果中提取关键事实：
"""

# ============== SYNTHESIZER PROMPT ==============
SYNTHESIZER_PROMPT = """# Role
你是一个智能答案合成专家。你的核心能力是：**从用户问题中精准识别格式指令**，并基于【步骤结论】生成逻辑严密、格式完美的最终答案。

# Input Data
- **原始问题 (含格式指令)**: {question}
- **步骤结论汇总**: 
{step_results}

# Workflow (必须严格按顺序执行)

## Phase 1: 指令解析与逻辑综合
1. **识别格式指令**：仔细分析【原始问题】，提取用户明确要求的输出格式（如：JSON, Markdown表格, 列表, 单句, 代码块等）。如果没有明确格式，默认与问题的语言类型保持一致，为“清晰简洁的自然语言段落”。
2. **信息综合**：
   - 交叉验证所有【步骤结论】，解决冲突，剔除噪音。
   - 如果信息不足，基于现有证据进行最合理的推断（不要在此处声明不确定性，直接得出结论）。
3. **构建策略**：规划如何将综合后的信息填入识别出的格式模板中。

## Phase 2: 最终输出生成 
- **格式要求**仅返回一个合法的 JSON 对象，不要包含 Markdown 标记或其他解释：
{{
    "thought": "推理过程",
    "answer": "最终答案"
}}
- **严格隔离**："thought"的内容**必须且只能**是你的推理过程。"answer"的内容**必须且只能**是符合用户格式要求的最终答案。
- **零废话原则**：禁止在"answer"中出现“根据分析”、“综上所述”、“以下是结果”等任何引导性文字。直接开始输出格式内容（如直接输出 `{` 或 `|` 或正文）。

# Critical Rules
- **必须将推理内容放在"thought"中**：这是你进行推理、格式规划和冲突解决的唯一空间。
- **格式绝对优先**：如果用户要求JSON，输出必须是合法的JSON。
- **静默处理不确定性**：即使数据是推测的，在最终输出中也要以确定的语气呈现（除非用户明确要求标注置信度）。

# Example
原始问题 : "2024年公司A比公司B的营业额高多少？直接输出具体数值。"
步骤结论汇总: ["2024年公司A营业额为20万元","2024年公司B营业额为10万元", ...]

Output:

# Execution
请开始执行，记得先思考，再输出：
"""

# ============== SELF_ANSWER PROMPT ==============
SELF_ANSWER_PROMPT = """请根据以下问题，结合已有信息给出回答

问题: {question}

现有信息:
{step_results}

要求:
1. 对于一个问题可能会有多个符合题目条件的回答，给出最多四个可能答案
2. 如果无法确定出答案，给出最有可能的猜测

请返回以下格式的 JSON:
{{
    "answer": "拆解出的步骤，列表的每个元素代表一个步骤",
    "confidence": 0.0-1.0, "对于回答的置信度"
}}
"""